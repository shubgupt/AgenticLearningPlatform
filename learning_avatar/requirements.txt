fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
openai==1.40.0
mcp==1.9.0
python-dotenv==1.0.1
pyyaml==6.0.2
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2

# pip fallback install for this src-layout package:
#   pip install -r requirements.txt
#   pip install -e .
# (equivalent to `uv sync` if you have uv installed instead)
